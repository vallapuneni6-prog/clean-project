# Allow PHP files in /api to execute directly
RewriteEngine Off

# Disable directory listing
Options -Indexes

# Allow all requests to PHP files
<FilesMatch "\.php$">
    Allow from all
</FilesMatch>
